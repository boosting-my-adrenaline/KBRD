import React, { useRef } from 'react'

interface IProps {
  overall: number
  CPM: number
  fti: number[]
}

export const BOOKstatsRecorder: React.FC<IProps> = ({ overall, CPM, fti }) => {
  // let highestSPM =

  return <> </>
}
